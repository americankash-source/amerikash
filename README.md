# AmeriKash Agent System

FastAPI financial agent backend for cashflow, risk, and investment planning.

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Test

```bash
pytest -q
```
